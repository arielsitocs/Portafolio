import type { IconType } from "react-icons";

interface TechnologyTypes {
  name: string;
  icono: IconType;
  iconSize?: number;
  iconColor?: string;
  color: string;
}

export default TechnologyTypes;
