
<form action="https://formsubmit.co/24b60ba7ba2ccc74c838de11763f196d" method="POST" id="form">
    <div class="nombre">
        <h1>Nombre</h1>
        <input type="text" name="name" minlength="3" required/>
    </div>

    <div class="correo">
        <h1>Correo</h1>
        <input type="email" name="email" required/>
    </div>

    <div class="mensaje">
        <h1>Dejame tu mensaje</h1>
        <textarea name="subject" minlength="10" required></textarea>
    </div>

    <div class="boton">
        <button type="submit">ENVIAR</button>
    </div>
    
    <input type="hidden" name="_next" value="https://arieltesting.netlify.app/#inicio">
    <input type="hidden" name="_captcha" value="false">
</form>

<style>
    form {
    font-family: 'Ubuntu', sans-serif;
    margin-top: 20px;

    .nombre, .correo, .mensaje {
        margin-bottom: 10px;
    }

    h1 {
        color: white;
        margin-bottom: 8px;
        font-size: 17px;
    }

    input,
    textarea {
        background-color: var(--gray-black);
        border: none;
        font-size: 14px;
        padding: 7px;
        color: white;
        width: 100%;
        height: 40px;
        resize: none;
    }
    
    input:active {
        border: 2px solid var(--diamond);
    }

    .mensaje {
        textarea {
            height: 150px;
        }
    }

    .boton {
        display: flex;
        justify-content: center;
    }

    button {
        width: 120px;
        height: 35px;
        margin-top: 10px;
        margin-bottom: 10px;
        font-weight: 600;
        background-color: var(--diamond);
        color: white;
        border: none;
        border-radius: 7px;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    button:hover {
        transform: scale(1.1);
        box-shadow: 0 0 10px var(--diamond);
        cursor: pointer;
    }
}
</style>